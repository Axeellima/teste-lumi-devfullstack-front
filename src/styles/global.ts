import styled from "styled-components"

const TitleDetachText = styled.h1`
  font-size: 22px;
  font-weight: 700;
`
const TitleText = styled.h1`
  font-size: 22px;
  font-weight: 500;
`
const TitleLightText = styled.h1`
  font-size: 22px;
  font-weight: 300;
`

const SubtitleText = styled.h2`
  font-size: 14px;
  font-weight: 500;
`

const SubtitleDetachText = styled.h2`
  font-size: 14px;
  font-weight: 700;
`
const SubtitleLightText = styled.p`
  font-size: 14px;
  font-weight: 300;
`
const DescriptionText = styled.p`
  font-size: 12px;
  font-weight: 700;
`
const DescriptionLightText = styled.p`
  font-size: 12px;
  font-weight: 300;
`
const DescriptionDetachText = styled.p`
  font-size: 12px;
  font-weight: 700;
`
const DetachText = styled.strong``
const SpecialText = styled.h1``

export {
  TitleDetachText,
  TitleText,
  SubtitleDetachText,
  SubtitleText,
  SubtitleLightText,
  DescriptionDetachText,
  DescriptionLightText,
  DescriptionText,
  TitleLightText,
}
